<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Nouara — Je t'aime</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700;900&family=Montserrat:wght@300;500&display=swap" rel="stylesheet" />

    <style>
      :root {
        --rouge: #e5093a;
        --rose: #ff7ea8;
        --fond: #fff5f8;
        --texte: #2b0b12;
      }

      *, *::before, *::after {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        color: var(--texte);
        font-family: 'Montserrat', system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
        background:
          radial-gradient(circle at 20% 20%, var(--rose) 0%, transparent 40%),
          radial-gradient(circle at 80% 30%, #ffc2d1 0%, transparent 40%),
          linear-gradient(135deg, #ffe2ea, #ffd6e5 60%, var(--fond));
      }

      .main {
        text-align: center;
        padding: 3rem 2rem;
        background: rgba(255, 255, 255, 0.6);
        backdrop-filter: blur(4px);
        border-radius: 20px;
        box-shadow: 0 20px 60px rgba(229, 9, 58, 0.15);
      }

      h1 {
        margin: 0 0 0.5rem;
        font-family: 'Playfair Display', serif;
        font-size: clamp(2.6rem, 6vw, 5rem);
        line-height: 1.1;
        color: var(--rouge);
      }

      p {
        margin: 0;
        font-size: clamp(1rem, 2.5vw, 1.25rem);
        color: #7a1a2c;
      }

      .heart {
        width: 22px;
        height: 22px;
        display: inline-block;
        position: relative;
        transform: rotate(-45deg);
        margin: 0 8px;
        background: var(--rouge);
      }

      .heart::before,
      .heart::after {
        content: "";
        position: absolute;
        width: 22px;
        height: 22px;
        background: var(--rouge);
        border-radius: 50%;
      }

      .heart::before { top: -11px; left: 0; }
      .heart::after  { left: 11px; top: 0; }

      .footer {
        position: fixed;
        bottom: 16px;
        left: 0;
        right: 0;
        text-align: center;
        font-size: 0.9rem;
        color: #a83a55;
      }
    </style>
  </head>

  <body>
    <div class="main">
      <h1>Je t'aime ma femme</h1>
      <p>Pour toi, Nouara <span class="heart" aria-hidden="true"></span></p>
    </div>
    <div class="footer">
      Toujours et pour toujours.
    </div>
  </body>
</html>
